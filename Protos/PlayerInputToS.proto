syntax = "proto3";

import "PlayerInput.proto";
import "ProtoTransform.proto";

// @auto_generate
message PlayerInputToS
{
	string entityId = 1;
	int64 tick = 2;
	PlayerInput playerInput = 3;
	ProtoTransform entityTransform = 4;
}