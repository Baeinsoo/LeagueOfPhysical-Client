# 캐주얼 UI 테마 — 폴가이즈 톤 공통 스타일

프론트엔드·인게임 UI 전반에서 "덜 만든 티"를 걷어낸다. **새 이미지 에셋 0장**으로,
색·테두리·라운드·폰트만 손본다. 최종 아트가 아니라 **최종 아트가 와도 버려지지 않는 바닥**을
까는 작업이다.

## 1. 배경 — 왜 더미로 보이나

원인은 **아트가 없어서가 아니다.** 화면 8개의 USS를 전수 확인한 결과, 원인은 다섯이고 넷은
이미지 없이 해결된다.

| 원인 | 근거 |
|---|---|
| **버튼이 유니티 기본 스킨** | `.login-button`·`.lobbyhome-play`·`.matching-cancel`이 크기·폰트만 지정. `background-color`도 `:hover`/`:active`도 없어 회색 기본 버튼이 그대로 노출 |
| **폰트가 기본 폰트** | "상점"·"프로필"·"◀ 뒤로" 등 한글이 있는데 전용 폰트 미지정 |
| **색이 화면마다 복붙** | `rgb(18,18,24)` / `rgb(30,30,38)` / `rgba(20,20,28,0.92)` / `rgba(20,20,28,0.95)` — 거의 같은데 미묘하게 다름. `MatchResultView.uss`와 `MatchingWaitingView.uss`는 클래스 이름만 다르고 값이 100% 동일 |
| **톤이 게임과 반대** | 배경 PNG(치비·파티·고채도) 위에 검정 반투명 카드 = 다크 관리자 대시보드 톤 |
| **움직임 0** | 등장·눌림 전환 없음 |

즉 **스타일이 지정되지 않은 것**이지 에셋이 없는 게 아니다. 이 문서는 그 스타일을 채운다.

## 2. 결정 요약

| 결정 | 내용 |
|---|---|
| 톤 = **폴가이즈 쪽** | 플랫 고채도 + 두꺼운 흰 테두리 + 아래쪽 두께. 그라데이션을 안 써서 순수 USS로 재현됨 (§4) |
| 브롤스타즈 톤 기각 | 그라데이션·광택이 USS에 없어 9-slice 스프라이트 5장+ 필요. 최종 아트 미정 단계라 보류 (§3-2) |
| Meshy 등 3D 생성 도구 기각 | UI는 3D 생성기의 산출물이 아님 (§3-1) |
| 배색 = **타이틀 로고에서 추출** | 주 CTA 주황 `#F5931E`. 로고와 UI가 한 세트로 보이게 (§5) |
| 색을 **토큰 한 곳**에 모음 | `Theme.uss`의 `:root` 변수. 화면 USS에는 레이아웃만 남김 (§6) |
| 적용 지점 = **PanelSettings의 TSS** | 화면마다 붙이지 않고 전역 1회 (§6) |
| 폰트 = **주아체 (Jua), SDF** | 글자 테두리·그림자가 SDF 폰트에서만 동작 (§8) |
| 입체감은 `box-shadow` 아님 | USS에 없음. 아래쪽 테두리 + 필요 시 바닥면 자식 요소 (§7) |
| DebugHud 제외 | 개발용이라 꾸미면 가독성이 나빠짐 (§9) |
| GamePad 아이콘 제외 | 이미지가 필요한 유일한 항목. 글자(DASH/JUMP) 유지 (§9) |

## 3. 검토 후 기각한 대안

### 3-1. Meshy 등 생성 AI로 UI 제작

**Meshy는 텍스트/이미지 → 3D 메시 생성기다.** 산출물이 `.glb`라 버튼 테두리·패널 프레임 같은
2D 부품이 나오지 않는다. 3D가 UI에 쓰이는 자리는 ①입체 아이콘을 렌더해 스프라이트로 굽기,
②로비 캐릭터 미리보기(RenderTexture) 둘뿐이고, 둘 다 이 작업의 범위가 아니다.

2D 이미지 생성 AI는 성립하지만 이 단계에선 ROI가 낮다 — 9-slice로 쓰려면 네 모서리가 정확히
대칭이고 알파 경계가 깨끗해야 하는데 생성 AI가 가장 취약한 종류라 수작업 보정이 붙는다.

### 3-2. 브롤스타즈 톤 (스프라이트 기반)

기각이 아니라 **보류**다. 목업으로 비교했고 화려함은 확실히 우위였다. 다만 재료가 USS에 없다:

| 재료 | UI Toolkit |
|---|---|
| 버튼 그라데이션 | ❌ USS에 `linear-gradient` 없음 |
| 요소 그림자 | ❌ `box-shadow` 미지원 |
| 금속 트림·광택 | ❌ |
| 글자 테두리 / 글자 그림자 | ✅ `-unity-text-outline-*` / `text-shadow` |

필요 에셋은 버튼 3종 + 패널 1 + 칩 1 + 아이콘 5~8. 9-slice라 버튼 1장이 모든 크기를 커버해
양 자체는 적지만, **최종 아트 방향이 정해지지 않은 지금 만들면 버려질 확률이 높다.**

> 나중에 올려도 손해가 없다. 이 문서의 구조·색·클래스는 그대로 두고 `background-image`만
> 추가하는 작업이라 **재작업이 아니라 덧칠**이다.

### 3-3. 어두운 톤 유지 / 반투명 유리판

- **어두운 톤**: 글자 가독성은 최고지만 배경 위에 어두운 막이 필요해 **애써 만든 배경이 죽는다.**
- **반투명 유리**: 유리 느낌의 핵심인 배경 흐림(blur)이 **UI Toolkit에 없다.** 반투명 흰 판까지가
  최대치이고, 그 결과물은 파티 톤이 아니라 모던·앱스토어 톤이다.

## 4. 확인된 UI Toolkit 능력 (설계 전제)

Unity `6000.3.16f1` 기준으로 확인했다.

| 기능 | 지원 | 이 설계에서 |
|---|---|---|
| USS 변수 `--x` + `var(--x)` | ✅ | 색·치수 토큰 |
| Theme Style Sheet(`.tss`) + PanelSettings | ✅ | 전역 적용 지점 |
| `-unity-text-outline-width` / `-color` | ✅ (SDF 폰트 한정) | 글자 테두리 |
| `text-shadow` | ✅ (SDF 폰트 한정) | 글자 그림자 |
| `border-*-width` 개별 지정 | ✅ | 아래쪽 두께 |
| `translate` + `transition` | ✅ | 눌림 반응 |
| `linear-gradient` | ❌ | 안 씀 |
| `box-shadow` | ❌ | 자식 요소로 대체 |
| 배경 흐림(blur) | ❌ | 안 씀 |

> 변수 제약: `var()`를 다른 함수 안에 넣을 수 없고 연산도 안 된다. 파생값(예: 진한 버전)은
> 별도 토큰으로 둔다.

프로젝트 설정도 확인했다 — `UIPanelSettings.asset`이 `Scale With Screen Size` / 기준 해상도
**1920×1080** / `Match: 0`(가로폭 기준)이다. 따라서 **USS의 px 값은 1920×1080 좌표계**로 쓴다.

## 5. 팔레트 — 타이틀 로고에서 추출

`Assets/UI/Title/title_screen.png`와 `Assets/UI/Lobby/lobby_screen.png`에서 뽑았다.
"완성감이 없다"의 원인 중 하나가 **로고 따로 UI 따로**인 상태였고, 이 배색이 그걸 없앤다.

| 역할 | 색 | 짝이 되는 진한 색 (바닥면·글자 테두리) |
|---|---|---|
| 주 CTA (PLAY, 로그인, 확인) | `#F5931E` 주황 | `#B4510F` |
| 보조 (취소, 뒤로, 네비 1) | `#4A9FE0` 하늘파랑 | `#2C6FA8` |
| 네비 2 | `#5FC94A` 초록 | `#389030` |
| 네비 3 | `#A87BD8` 보라 | `#6F51B8` |
| 카드·패널 표면 | `#FDF6EA` 크림 | 테두리 `#C25A12` |
| 본문 글자 | `#5A3A1E` | — |
| 채움 위 글자 | `#FFFFFF` | — |
| 요소 테두리 | `#FFFFFF` | — |
| 타이틀 글자 | `#FFC93C` | 테두리 `#B4510F` |

카드가 **검정 반투명 → 크림색**으로 뒤집히는 것이 가장 큰 변화다.

## 6. 구조 — 색은 한 곳, 화면은 얇게

```
UIPanelSettings.asset  (themeUss)
   └─ Assets/UI/Theme/LopTheme.tss
        ├─ @import url("unity-theme://default")   ← 필수. 빼면 기본 컨트롤이 깨짐
        └─ @import  Theme.uss
             ├─ :root { --lop-* }        ← 색·치수 토큰
             └─ .btn / .card / .pill …   ← 공통 부품
```

- **전역 적용은 TSS 한 곳**에서 끝난다. 화면마다 `<Style src>`를 붙이지 않는다.
- TSS가 `@import`로 다른 USS를 참조하는 것은 유니티 문서가 명시한 표준 용법이다. 프로젝트 에셋을
  가리키는 정확한 URL 표기는 구현 시 UI Builder가 생성하는 형태를 따른다.
- **각 화면 USS에는 레이아웃(위치·크기·정렬)만 남긴다.** 색·테두리·라운드·폰트는 전부 공통 클래스로
  옮긴다. 이후 색 변경은 토큰 한 줄 수정 = 8화면 동시 반영.
- 중복인 `MatchResultView.uss` / `MatchingWaitingView.uss`는 공통 `.card` 계열로 흡수한다.

### 토큰

색은 §5 표를 `--lop-primary`, `--lop-primary-shade`, `--lop-secondary`, … 형태로 옮긴다.
치수 토큰은 1920×1080 기준의 시작값이며 **구현 중 눈으로 보고 조정한다.**

| 토큰 | 시작값 | 쓰임 |
|---|---|---|
| `--lop-border` | `12px` | 요소 흰 테두리 (주 CTA) |
| `--lop-border-s` | `8px` | 네비·알약 |
| `--lop-bevel` | `24px` | 아래쪽 두께 (주 CTA) |
| `--lop-bevel-s` | `16px` | 네비·알약·카드 |
| `--lop-radius-pill` | `999px` | 주 CTA·재화 알약 |
| `--lop-radius-l` | `44px` | 네비 버튼 |
| `--lop-radius-m` | `36px` | 카드·패널 |

### 공통 부품

| 클래스 | 무엇 |
|---|---|
| `.btn` | 버튼 공통 — 흰 테두리, 아래쪽 두께, 라운드, 글자 테두리, 눌림 전환 |
| `.btn--primary` | 주황 CTA |
| `.btn--secondary` | 하늘파랑 |
| `.btn--nav-1/2/3` | 하단 네비 3색 |
| `.card` | 크림색 패널 |
| `.pill` | 상단 재화·레벨 알약 |
| `.title` | 큰 제목 글자 (테두리 + 그림자) |
| `.btn__base` | 주 CTA 뒤에 까는 바닥면 요소 (§7) |

UXML도 함께 수정된다 — 예: `<ui:Button class="lobbyhome-play">` →
`<ui:Button class="btn btn--primary lobbyhome-play">`. 화면 USS의 `.lobbyhome-play`에는
위치·너비만 남는다.

## 7. 입체감 — `box-shadow` 없이

| 단계 | 방법 | 쓰는 곳 |
|---|---|---|
| 기본 | **아래쪽 테두리만 두껍게** (`border-bottom-width: var(--lop-bevel-s)`) | 네비 버튼, 알약, 카드 |
| 강조 | 버튼 뒤에 **바닥면 역할의 자식 `VisualElement` 1개** | 주 CTA만 |

2단계는 UXML에 요소 한 줄이 추가된다. 남용하지 않고 **주 CTA에만** 쓴다.

## 8. 눌림 반응

모바일이라 마우스 오버보다 **눌림이 중요**하다.

- `:active` 상태에서 버튼을 아래로 내리고(`translate`) 바닥 두께를 줄인다 → 실제로 눌린 것처럼 보임
- `transition-duration`은 짧게(60~90ms). 길면 조작이 굼떠 보인다
- `:hover`는 밝기만 살짝. PC 테스트용이고 모바일에선 안 보인다

전부 USS로 끝나고 C# 변경이 없다.

## 9. 폰트

**주아체(Jua)** 를 쓴다. Google Fonts에 OFL로 올라와 있어 상업 이용이 가능하고 한글을 지원한다.

- TTF → **Font Asset(SDF)** 으로 변환 후 `:root`의 `-unity-font-definition`에 지정
- **SDF여야 한다** — 유니티 문서상 글자 테두리·그림자 효과가 SDF 렌더 모드에서만 동작한다.
  비트맵으로 만들면 목업의 글자 테두리가 나오지 않는다
- 한글은 Font Asset을 **Dynamic 모드**로 만든다 — 한글 음절이 많아 전부 굽지 않고 필요한 글자만
  그때그때 아틀라스에 올린다 (Font Asset 자체의 설정이며, PanelSettings의 동적 아틀라스와는
  별개 항목이다)
- 주아체에 없는 글자를 대비해 **폴백 폰트**를 지정한다

## 10. 적용 범위

| 화면 | 하는 일 |
|---|---|
| Login · LobbyHome · Shell · MatchingWaiting · GameLoading · MatchResult · Stats | 공통 부품으로 교체. 카드를 크림색으로 전환 |
| CharacterHud | 체력·마나·경험치 바에 흰 테두리 + 라운드. **바 색은 유지** (HP=빨강 등은 장르 관습이라 건드리지 않음) |
| GamePad | 조이스틱·액션 버튼에 테마 적용. **아이콘은 만들지 않음** — 글자(DASH/JUMP/ATK) 유지 |
| Nameplate · DamageFloater | 글자 테두리로 배경과 분리 |
| **DebugHud** | **손대지 않음** — 개발용이라 꾸미면 정보 가독성이 나빠진다 |

## 11. 검증

스타일·에셋 작업이라 자동 테스트 대상이 아니다. 육안으로 한다.

| 항목 | 방법 | 기대 |
|---|---|---|
| 톤 일치 | 8화면을 차례로 띄워 스크린샷 | 한 게임의 UI로 보임. 로고와 버튼 색이 같은 계열 |
| 기본 스킨 잔존 | 전 화면의 모든 버튼 확인 | 회색 유니티 기본 버튼이 하나도 남지 않음 |
| 한글 렌더 | "상점·프로필·설정·뒤로·매칭 중" | 글자가 깨지거나 네모로 나오지 않음 |
| 글자 테두리 | 타이틀·버튼 라벨 | 테두리와 그림자가 실제로 보임 (= SDF 설정이 맞음) |
| 눌림 반응 | 각 버튼을 눌러봄 | 눌리는 동안 내려갔다 돌아옴 |
| 기본 컨트롤 | 스크롤 있는 화면(Stats 등) | 스크롤바가 정상 렌더 (= 기본 테마 import 유지 확인) |
| 비율 | 기준 외 화면비(세로로 긴 폰) | 하단 네비바가 잘리지 않음 |

## 12. 리스크

| 리스크 | 대응 |
|---|---|
| 화면 매칭이 **가로폭 기준**(`Match: 0`)이라 세로로 긴 비율에서 아래쪽이 잘릴 수 있음 | 하단 요소를 화면 바닥에 붙여 배치. 실기기 비율에서 확인(§11) |
| 기본 테마 import 누락 시 기본 컨트롤이 깨짐 | `@import url("unity-theme://default")` 유지를 §6에 못박음. 검증 항목에도 포함 |
| SDF 아닌 폰트로 만들면 글자 테두리가 안 나옴 | §9에 명시. 검증 항목에 포함 |
| 주아체 글리프 미커버 | 폴백 폰트 지정 |
| 치수 토큰의 시작값이 과하거나 부족할 수 있음 | §6 표는 **시작값**이며 구현 중 눈으로 조정 |
| 색을 토큰으로 모으면서 화면 고유 의도를 지울 위험 | HP/MP 바 색처럼 **의미가 있는 색은 유지**(§10) |

## 13. 산업 표준 매핑

| 우리 결정 | 대응하는 표준 |
|---|---|
| 색·치수를 토큰으로 모으고 화면은 참조만 | 디자인 토큰(Design Tokens) — 웹/모바일 디자인 시스템의 정석. USS 변수가 CSS 커스텀 프로퍼티에 대응 |
| 전역 스타일을 PanelSettings의 TSS 한 곳에 | UI Toolkit이 문서상 지정한 런타임 전역 테마 적용 지점 |
| 공통 `.btn` + 변형 `.btn--primary` | BEM 수정자(modifier) 규약 |
| 아래쪽 테두리로 입체감 | 캐주얼 모바일 게임 버튼의 표준 처리. `box-shadow`가 없는 환경의 우회가 아니라 원래 통용되는 기법 |
| 연속 축이 아닌 **상태**에 `:active` 전환 | UI Toolkit 의사 클래스 표준 용법 |

## 14. Open Decisions

- [ ] **GamePad 아이콘** — 액션 버튼(DASH/JUMP/ATK/AOE/HASTE)은 글자보다 아이콘이 맞다. 이미지가
      필요한 유일한 항목이라 이번 범위 밖. 무료 CC0 팩 / 직접 제작 중 선택은 착수 시점에
- [ ] **브롤스타즈 톤으로의 승급** — §3-2. 최종 아트 방향이 정해질 때. 이 문서의 구조는 그대로 두고
      `background-image`만 얹는 작업
- [ ] **타이틀 로고 이미지 재사용** — `title_screen.png`에 이미 로고가 있다. 알파로 잘라내 쓰면
      글자 스타일 고민이 사라지지만 이미지 편집이 필요. 이번엔 USS 글자로 간다
- [ ] **화면 등장 애니메이션** — 눌림 반응까지만 이번 범위. 화면 전환 시 페이드/슬라이드는 윈도우
      매니저 레이어의 일이라 별도
